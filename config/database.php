<?php
/**
 * Database credentials for the local XAMPP environment.
 *
 * Each developer keeps their own local copy of this file.
 * This file is listed in .gitignore so teammates don't overwrite each other.
 *
 * Default XAMPP values:
 *   host     = localhost
 *   user     = root
 *   password = (empty string)
 */
return [
    'host'     => 'localhost',
    'port'     => 3306,
    'dbname'   => 'tara_pangasinan',
    'user'     => 'root',       // change to 'tara_user' if you created a dedicated account
    'password' => '',           // change to your password if applicable
    'charset'  => 'utf8mb4',
];
